name: meta
parent: default

matches:
  - trigger: ":match:"
    replace: "  - trigger:\n    replace:"
  - trigger: ":PL:"
    replace: |
             ```--------```
             *Estacionamento* {{d}} (car) (truck)
             -
             ```--------```
             Log (clock):
             
             ```--------```
    vars:
     - name: d
       type: date
       params:
         format: "%d/%m/%Y"
