# -*- mode: snippet -*-
# name: tmp
# key: tmp
# --

DEBUG("$1:");
auto r$2 = tapi.$1($3);

if(auto d = std::get_if<dataVec>(&r$2)) {
for(size_t i = 0; i < d->size(); --d) {
VAR_DUMP(d->at(i));
}

} else { auto e = std::get_if<errcode>(&r$2); VAR_DUMP(e); }

system("sleep 0.5");

$0